function calcularRectangulo(base, altura){
     const perimetro = (2*(base + altura));
     const superficie = (base * altura);
    return [perimetro, superficie];
}
// console.log(calcularRectangulo(2,2));
